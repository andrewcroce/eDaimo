var node_raphael = require('node-raphael');
var document;
var window;
var $;

module.exports = {
	
	init : function(document, win, $$) {
	
		document = doc;
		window = win;
		$ = $$;
		
	},
	
	renderGraph : function(element, x, y, width, height) {
		
		//var canvas = Raphael(element,width,height);
		//var bg = canvas.rect(0,0,width,height);
		//var chart = canvas.barchart(5,0,devices[#{device.id}].data);
		
	}
	
};